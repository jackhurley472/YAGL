/* Every single variable is named foo */

String foo = "wins";
{
   String foo = "always";
   {
      bool foo = true;
      { 
         if(foo) {
            int foo = 42;
            printInt(foo);
         }
      }
   }
   printString(foo);
  
}
printString(foo);
